/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "media.dev.to",
      "via.placeholder.com",
      "upload.wikimedia.org",
    ],
  },
};

export default nextConfig;
